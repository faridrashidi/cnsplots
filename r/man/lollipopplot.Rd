% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots-categorical.R
\name{lollipopplot}
\alias{lollipopplot}
\title{Plot aggregated values as lollipops}
\usage{
lollipopplot(
  data,
  x,
  y,
  hue = NULL,
  order = NULL,
  hue_order = NULL,
  pairs = NULL,
  add_tip = FALSE,
  estimator = "mean",
  errorbar = NULL,
  markersize = 20,
  linewidth = 1.5,
  marker = "o",
  dodge = 0.8,
  color = NULL,
  palette = NULL,
  baseline = 0
)
}
\arguments{
\item{data}{A data frame.}

\item{x, y}{Column names. One column supplies categories and the other must be
numeric.}

\item{hue}{Optional grouping column used for dodged lollipops.}

\item{order}{Optional category order.}

\item{hue_order}{Optional hue order.}

\item{pairs}{Reserved for Welch-test annotations. Non-\code{NULL} values currently
fail explicitly rather than being ignored.}

\item{add_tip}{Add a two-decimal value label at each observed marker.}

\item{estimator}{Either \code{"mean"} or \code{"median"}.}

\item{errorbar}{One of \code{NULL}, \code{"se"}, \code{"sd"}, or \code{"ci"}.}

\item{markersize}{Matplotlib-compatible marker area in squared points.}

\item{linewidth}{Stem width in points.}

\item{marker}{Matplotlib-style marker code for a supported common shape.}

\item{dodge}{Total grouped-lollipop width.}

\item{color}{Optional single colour overriding \code{palette}.}

\item{palette}{A registered palette name or colour vector.}

\item{baseline}{Value from which stems start.}
}
\value{
A ggplot2 plot.
}
\description{
This is the native ggplot2 counterpart of the author's lollipop recipe.
It automatically uses a horizontal layout when \code{x} is numeric and \code{y} is
categorical. Mean intervals use the sample standard deviation, standard
error, or a Student 95 percent confidence interval. Median standard errors
and confidence intervals use a deterministic 1,000-resample bootstrap that
restores the caller's random-number state.
}
