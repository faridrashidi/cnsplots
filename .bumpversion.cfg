[bumpversion]
current_version = 0.0.0
commit = True
message = {current_version} → {new_version}
tag = True
files = setup.py cnsplots/__init__.py
parse = (?P<major>\d+)\.(?P<minor>\d+)\.(?P<patch>\d+)
serialize = 
	{major}.{minor}.{patch}
